namespace RouteParser {
namespace Units {
enum Speed { KNOTS, MACH, KMH };
enum Temperature { CELSIUS, FAHRENHEIT };
enum Pressure { HPA, INHG };
enum Distance { NAUTICAL_MILES, KILOMETERS, FEET, METERS };
} // namespace Units

enum FlightRule { IFR, VFR };
} // namespace RouteParser